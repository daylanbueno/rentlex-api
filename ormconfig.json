{
    "type": "postgres",
    "host": "localhost",
    "username": "docker",
    "password": "ignate",
    "database": "rentx",
    "migrations": ["./src/database/migrations/*.ts"],
    "cli": {
        "migrationsDir": "./src/database/migrations"
    }
}
